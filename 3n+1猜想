描述：对于任意自然数n，若为偶数，则将其砍掉一半，若为奇数，则将（3n+1）砍掉一半。一直执行，直到n=1。
#include <iostream>
using namespace std;
int main()
{
    int n,steps=0;
    cin>>n;
    while(n!=1)
    {
        if(n%2==0)
        {
            n=n/2;
            steps++;
        }
        else{
            n=(3*n+1)/2;
            steps++;
        }
    }
    cout<<steps<<endl;
    return 0;
}
